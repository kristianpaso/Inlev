# Rotation project

## Paths (as requested)
- Backend: `localhost/public/rotation-api`
- Frontend: `localhost/public/rotation`

This zip contains two folders:
- `rotation-api/` (Express backend)
- `rotation/` (static frontend)

## Next step
1) Copy `rotation/` into your existing Netlify site under `public/rotation/`.
2) Deploy `rotation-api/` to Render.
3) In the UI, set API Base URL to your Render URL.

