{
  "name": "inlev-fullpack-v2.1",
  "private": true,
  "scripts": {
    "dev": "netlify dev --dir=public",
    "deploy:prod": "netlify deploy --build --prod --dir=public"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "dependencies": {
    "@netlify/blobs": "^6.3.0"
  }
}